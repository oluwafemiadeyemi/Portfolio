# Choose your provider: ollama (free, local), anthropic, or groq
PROVIDER=ollama

# Only needed if PROVIDER=anthropic
ANTHROPIC_API_KEY=

# Only needed if PROVIDER=groq (free tier at console.groq.com)
GROQ_API_KEY=
